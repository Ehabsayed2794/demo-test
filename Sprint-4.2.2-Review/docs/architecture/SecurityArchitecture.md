# Firestore Security Design

Originally design-only. **As of Sprint 3.3, the `players/{uid}` and `rooms/{roomId}` rows below describe what is actually implemented and shipped in `firestore.rules`** — this document was re-synced to close the drift the Sprint 3.2.5 Architecture Audit found (the `rooms` row previously described a `hostUid`/seat-based design that was never built; see `docs/implementation/ReadyStateFoundation.md`). **As of Sprint 3.4, the `matches/{matchId}` row is likewise re-synced to the actual, minimal shipped rule** — see the callout directly below the table. **Sprint 3.4.1 (Match Start Consistency & Security Hotfix)** tightened both the `rooms/{roomId}` update rule and the `matches/{matchId}` create rule to cross-check the two documents against each other (see the updated rows below and `docs/implementation/MatchInitialization.md`'s Sprint 3.4.1 section) — this closed a real gap, not a hypothetical one: the Sprint 3.4 rules validated each document's own shape in isolation and never verified a match was actually bound to a legitimate, same-transaction room start. **Sprint 3.8 (Gameplay Synchronization: Bidding Authority) gives `matches/{matchId}` its FIRST real update rule** — see the row below and the new "Bidding write authority" section beneath the table for the full account of who owns bids, who may write, who may read, and how version/concurrency work. **Sprint 4.2 (Online Card Synchronization) adds its SECOND update rule, `isValidCardSubmission()`; Sprint 4.2.1 (Pre-Write Card Authority & Desync Safety) hardens the CLIENT side of card writes (turn authority + pre-write engine legality, both enforced in `MatchService.submitCard()` before any write) and, per its own Task 4, formally assesses — and documents, rather than papers over — a real limitation in the RULES side: `isValidCardSubmission()` cannot prove `cardLog`'s earlier entries are unchanged. Sprint 4.2.2 (Atomic Card Turn Progression & Card-Log Desync Hardening) extends `isValidCardSubmission()` further to verify the caller owned the previous active turn and the new turn is a structurally valid seat UID or `null` — see "Card write authority" below the table for the full account of both sprints.** Every other row remains exactly what it always was: a forward-looking design for a collection that doesn't exist in code yet — `MigrationPlan.md` covers what changes once Cloud Functions become available for those.

**Guiding principle for this whole document:** under the Spark constraint, rules are the *only* server-side enforcement available. Every "who can write what" decision below should be read with an honest note about where rules-only enforcement is strong versus where it's a soft approximation pending the Blaze migration — stated explicitly, not glossed over.

| Collection | Read | Create | Update | Delete |
|---|---|---|---|---|
| `players/{uid}` — **implemented** | Any authenticated user (opponents' names/ranks must be visible) | The system, at first sign-in (`request.auth.uid == uid`, document must not already exist) | Owner only, **field-restricted**: `displayName`/`avatarInitial`/`lastSeenAt`/`currentRoomId`/`currentMatchId` are owner-writable; `coins`/`gems`/`rank`/`rp`/`wins`/`streak`/`level` are **not** owner-writable — those only change via the match-completion path (soft-enforced by rules today; see the honest limitation below). **Unchanged in Sprint 3.4.1** — this exact owner-only rule is what correctly denied `MatchService`'s Sprint 3.4 attempt to write `currentMatchId` onto every room player's profile; the fix (see `MatchInitialization.md`) moved responsibility to the write's own owner instead of loosening this rule. | Nobody (no account-deletion flow designed yet — out of scope, see `PlayerLifecycle.md`) |
| `rooms/{roomId}` — **implemented** | Any authenticated user (deliberately unrestricted for MVP simplicity — no "must be a member" check on read) | Any authenticated user, becoming `creator` (not `hostUid`) — field-whitelisted, exactly one initial member (`players.size() == 1`), `readyPlayers` must start empty, `status` must start `"waiting"` | A current-or-incoming member only, and only for `players`/`readyPlayers`/`status`/`creator`/`updatedAt`/`matchId` — each of `players`/`readyPlayers` may only change by the acting user adding/removing **themself** (never another player's entry, never a bulk edit). `creator` may be reassigned only to someone still present in `players` (supports `leaveRoom()`'s ownership transfer) — **known, documented, unclosed gap:** this does not fully prevent an existing member from self-reassigning `creator` outside of an actual leave event; closing that fully needs Cloud Functions (see `MigrationPlan.md`). `status` may only be `"waiting"`/`"closed"`/`"in_game"`, and only becomes `"closed"` when `players` is empty. `matchId` may only go from absent/null to a string in the SAME write that flips `status` to `"in_game"` — **Sprint 3.4.1** added a full cross-check here too: that write also now re-verifies the acting user is a real room member, every player was really ready beforehand, and (via `getAfter()`) a real, correctly-bound match document was actually created in the same atomic write. There is no per-seat `ready` field and no `hostUid` in the shipped schema — both were part of an earlier, unbuilt design. | Never — rooms close via `status: "closed"`, not deletion |
| `matches/{matchId}` — **partially implemented (Sprint 3.4: create/read; Sprint 3.4.1 tightened create; Sprint 3.8 adds the first real update — bidding only; Sprint 4.2 adds the second — card submission)** | Only players listed in `players[].uid` for this match (not public — hides the match from everyone else, though see the `hands` note in `FirestoreSchema.md` for why this doesn't fully hide cards from co-players) | Any authenticated user who is one of the document's own `players[]` (there is no separate "system" writer on Spark — the client that calls `MatchService.startMatch()` performs this write directly, inside the same transaction that also updates `rooms/{roomId}` — `createMatch()`, the unsafe alternative that bypassed this binding, was removed from the public API in Sprint 3.4.1), field-whitelisted to the actual shipped shape (`roomId`/`players`/`status`/`createdAt`/`currentRound`/`dealer`/`turn`/`gameState`/`seats`/`version`/`biddingOpen`/`bids`/`lastBidSeat`/`cardLog`/`lastCardSeat` — the last seven added across Sprint 3.8/4.2), `status` must be `"starting"`, `currentRound` must be `1`, `dealer`/`turn` must each be one of `players[]`, `seats` must be a valid bijection between real seat names and `players[]` (Sprint 3.8, Task 1 — see `SeatIdentityModel.md`), `version` must be `1`, `biddingOpen` must be `true`, `bids` must have exactly one null slot per real seat, `cardLog` must start empty and `lastCardSeat` must start null (Sprint 4.2), **plus (Sprint 3.4.1) the referenced room must exist, must have been `"waiting"` with everyone ready before this write, the match's `players[]` must equal the room's, and the room's own `matchId` (post-commit, via `getAfter()`) must equal this document's own id** — closing the Sprint 3.4 gap where a match could otherwise be fabricated independently of a legitimate room start | **Sprint 3.8: allowed for a bidding submission (`isValidBidSubmission()`). Sprint 4.2 adds a SECOND, mutually exclusive shape — a card submission (`isValidCardSubmission()`).** Only `bids`/`biddingOpen`/`version`/`lastBidSeat`/`updatedAt` (bid shape) or `cardLog`/`lastCardSeat`/`version`/`updatedAt` (card shape) may ever change, per write; `players`/`seats`/`dealer`/`turn`/`status`/`currentRound`/`gameState`/`roomId` remain structurally immutable through either path. See "Bidding write authority" and "Card write authority" below the table for the full breakdown of each, including Sprint 4.2.1's honest, documented `cardLog` prefix-immutability gap. Trick-resolution/scoring/turn-rotation still don't exist (`MatchService.resolveTrick`/etc. all still throw `Not implemented`) — there is still no write path for any of those. | Nobody directly — matches end via `MatchLifecycle.md`'s TTL-based archival, not an explicit delete |
| `matches/{matchId}/hands/{uid}` (if the split-subcollection design from `FirestoreSchema.md` is adopted) | Owner only | The system, at deal time | Owner only, and only to remove a played card (never to add one that wasn't dealt) | Never — cleared by TTL alongside the parent match |
| `leaderboards/{seasonId}/entries/{uid}` | Any authenticated user | The system, at first ranked match | Owner only, **and rules should clamp the delta** (see below) | Never |
| `inventory/{uid}` | Owner only | The system, at Profile Creation (empty) | Owner only, and only via a write that references a valid `shop/{itemId}` and (for real-money items, later) a matching `transactions/{txId}` | Never |
| `shop/{itemId}` | Any authenticated user | Admin only | Admin only | Admin only |
| `transactions/{txId}` | Owner only | Owner only — **flagged below as the weakest point in this whole design** | Never (immutable audit record) | Never |
| `friendRequests/{requestId}` | Either `fromUid` or `toUid` | `fromUid` only, and only if `fromUid == request.auth.uid` | `toUid` only, and only the `status` field | `fromUid` only, while `status == "pending"` (cancel) |
| `friends/{uid}/list/{friendUid}` | Owner only | The system, as part of accepting a friend request (writes both sides) | Never (relationship, not mutable state) | Owner only (unfriend) |
| `seasonData/{seasonId}` | Any authenticated user | Admin only | Admin only | Admin only |
| `dailyRewards/{uid}` | Owner only | The system, at first claim | Owner only, **and rules must check `request.time` against the stored `lastClaimedAt`** to prevent claiming twice in one day — this is one of the few cases where a rules-only clamp is genuinely sufficient, not just an approximation | Never |
| `notifications/{uid}/items/{notificationId}` | Owner only | **Any authenticated user acting as the event's sender** — e.g. a friend-request sender writes into the *recipient's* notification subcollection. This is a narrower, more carefully-scoped exception to "owner writes their own stuff" than anything else in this table, and worth a second look at implementation time | Owner only (marking read) | Owner only |

## Bidding write authority (Sprint 3.8)

Answering this document's own required questions directly, per the brief:

- **Who owns bids?** Each seat owns its own bid, and only its own. Ownership is defined by `matches/{matchId}.seats[seatId]` — the map `isValidNewMatch()` establishes once, at creation (see `SeatIdentityModel.md`), and which `isValidBidSubmission()` never permits to change afterward (`seats` is not in that rule's allowed-changed-keys list at all). There is no "match owner" or "dealer" write authority over another seat's bid — the dealer's own bid is owned by the dealer's own seat, exactly like every other seat.
- **Who may write?** Exactly one person per seat: the uid `seats[seatId]` names, and only for that seat, and only while `biddingOpen == true`, and only if that seat hasn't already bid. `MatchService.submitBid(matchId, seatId, bid)` is the one code path that produces this write — it derives the caller's own uid from `SessionService` internally (never a parameter), so there is no argument for a caller to misuse to write as someone else; `firestore.rules`' `isValidBidSubmission()` independently re-derives and re-checks the identical ownership fact server-side, from `request.auth.uid`, trusting neither the client's JS nor its own request body's `lastBidSeat` claim alone.
- **Who may read?** Unchanged since Sprint 3.4: only a player listed in the match's own `players[]` (`allow get`). Bids are visible to every seated player the moment they're written (a `bid` amount isn't a secret in this game the way a hand of cards is — see `FirestoreSchema.md`'s "hands problem," which bidding does not reopen or worsen).
- **How does version work?** `version` starts at `1` when the match is created and must increase by **exactly** `1` on every accepted write (`newData.version == oldData.version + 1`) — checked independently by the rule, not merely produced correctly by client code that could be buggy or malicious. This is the Task 2 "ordering guard, made real": combined with Firestore's own transaction-level optimistic concurrency (every write happens inside `db().runTransaction()`), a stale, duplicated, or out-of-order write is rejected both at the app layer (the transaction re-reads fresh data on conflict) and, independently, at the rules layer (a version that isn't exactly current+1 is denied outright, regardless of how it was produced).
- **How does concurrency work?** Two clients racing to write the SAME document (whether the same seat or different seats) are serialized by Firestore's transaction mechanism itself — whichever commits first wins that version number; the other's transaction callback is automatically re-run against the now-current document. `submitBid()`'s own checks (already-bid, seat ownership, bidding-open) are re-evaluated on every such retry, so a losing racer for the SAME seat correctly fails with `ALREADY_BID` rather than silently overwriting the winner, while two racers for DIFFERENT seats both succeed, serialized into two sequential version increments. See `docs/architecture/MatchSynchronization.md`'s Sprint 3.8 section and `tests/submit-bid.test.cjs`'s "two simultaneous bidders" tests (MOCKED) for the concrete, executable proof of both cases.

## Card write authority (Sprint 4.2, hardened Sprint 4.2.1, extended Sprint 4.2.2)

- **Who owns a card play?** Each seat owns its own submissions to `cardLog`, exactly like bids — ownership is the SAME `matches/{matchId}.seats[seatId]` map, still never re-assignable through any write path.
- **Who may write?** `MatchService.submitCard(matchId, card)` is the one code path — it takes NO `seatId` parameter at all (stricter than `submitBid()`'s "claim a seat, then verify" shape); the acting seat is resolved purely from the calling uid via `MatchAdapter.uidToSeat()`, so there is no argument for a caller to even ATTEMPT submitting as a seat they don't own. **Sprint 4.2.1 closed a Critical gap here**: the ORIGINAL Sprint 4.2 version of this function never checked WHOSE TURN it currently was at all — any seat-owner could write regardless of turn order. It now calls `MatchAdapter.assertLocalTurn()` — the SAME authority gate Sprint 4.1 built for exactly this purpose — BEFORE ever opening a Firestore transaction, rejecting `NOT_YOUR_TURN` with zero writes attempted if it isn't. See `docs/architecture/EngineAdapter.md`'s Sprint 4.2.1 section for the honest caveat this inherits: the gate's accuracy still depends on `matches/{matchId}.turn` actually being current during the PLAY phase, which nothing in this codebase writes back yet — the SAME pre-existing gap Sprint 4.1 already documented for bidding, not newly introduced or newly claimed to be fixed here.
- **Is the card itself checked for legality before it's written?** **Sprint 4.2.1 closed the SECOND Critical gap**: the original version validated only a GENERIC shape (a real suit, a real rank) and wrote first, leaving legality entirely to whichever client later replayed the log — meaning an engine-rejected card stayed in `cardLog` PERMANENTLY once written, since nothing purged it. `submitCard()` now asks the real `TableEngine.canPlayCard()` (a pure, non-mutating query against the SAME `isLegal()`/`state.turn`/`state.phase` checks `emit()` already performs) whether the play would be accepted, BEFORE the transaction opens — an illegal card is rejected `ILLEGAL_CARD` with zero writes attempted. `firestore.rules`' own `isValidCardSubmission()` still does NOT (and structurally cannot, without re-implementing `table-engine.js`'s follow-suit logic in CEL — exactly the "duplicate gameplay rules in Firestore" this whole project forbids) check legality server-side; the CLIENT-side pre-write gate is what actually closes this specific defect, not a rules change.
- **How does version work?** Identical mechanism to bidding — `version` must increase by exactly 1, checked independently by `isValidCardSubmission()`, layered on Firestore's own transaction-level optimistic concurrency.
- **Does the write also advance whose turn it is next? (Sprint 4.2.2)** Yes, atomically, in the SAME write as the card append — closing a Critical gap Sprint 4.2.1 left open: `submitCard()` previously appended the card but never updated `matches/{matchId}.turn`, so the NEXT seat's own submission was incorrectly rejected as `NOT_YOUR_TURN` (Sprint 4.2.1's own tests hid this with a test-only manual turn mutation between submissions — never a production write). `submitCard()` now calls a new, purely additive, non-mutating `TableEngine.previewPlay()` query BEFORE the transaction to learn the intended next seat/phase, and writes `cardLog`, `lastCardSeat`, `turn`, `cardPhase`, and `version+1` all in one `tx.update()` call. `isValidCardSubmission()` was extended IN PLACE (an UPDATE rule evolves in place in this project, unlike CREATE rules, which get versioned suffixes) to independently verify: the caller owned the PREVIOUS active turn (`oldData.turn == request.auth.uid`); the NEW turn is either `null` or a UID present among `oldData.seats`' own values (expressed via `.keys().exists(s, oldData.seats[s] == newData.turn)`, since CEL has no `.values()` method — an already-established workaround in this project, not a new gap); and `newData.cardPhase in ['PLAY', 'RESOLVING']`. **Honest, restated limitation**: this proves the new turn is STRUCTURALLY valid, never that it is the CORRECT next seat per follow-suit/turn order — that remains entirely client-authoritative in this Spark MVP.
- **Does a malformed or content-mismatched remote delivery get silently accepted? (Sprint 4.2.2)** No, on either count, closing two more gaps: `MatchAdapter.applyRemoteCard()` previously skipped a `MALFORMED_ENTRY` item and kept processing, later advancing its version/count registries past it — now it stops immediately and treats it as a desync exactly like `ENGINE_REJECTED` already was. It also previously accepted a same-seat "local echo" without checking whether the card itself matched — now it compares exact `suit`/`rank.v`; a same-seat, DIFFERENT-card delivery produces a structured `LOCAL_ECHO_MISMATCH` desync rather than being silently dropped as a benign echo.
- **HONEST, DOCUMENTED LIMITATION (Sprint 4.2.1, Task 4 — Card Log Integrity Assessment):** `isValidCardSubmission()` verifies `cardLog` grew by exactly one entry and that the new entry is well-formed and correctly attributed. It does **NOT** independently verify that every EARLIER entry is byte-for-byte unchanged or still in its original order. **This was formally assessed, not assumed**: CEL (Firestore Rules, `rules_version '2'`) has no documented primitive for comparing two lists index-by-index without a `range()`/slice() construct it does not have; the closest available tool, `oldLog.all(x, x in newLog)`, proves only multiset MEMBERSHIP, not POSITION — it would not catch a client reordering two existing entries, only a client removing one entirely. **This gap is demonstrated, not merely asserted**: `tests/rules-simulation.test.js`'s two "KNOWN VULNERABILITY" checks prove directly, against this project's own 1:1 rules translation, that BOTH rewriting an earlier entry's card value AND reordering two earlier entries currently pass `isValidCardSubmission()` unchanged, provided exactly one new, well-formed entry is also appended.
  - **Exact production risk**: a client acting as one of the real seated players in their OWN match could rewrite or reorder their own match's card-play history while it's in progress. Today this has NO exploitable consequence — no code in this codebase reads `cardLog` for scoring, trick resolution, or any stakes-bearing purpose (Sprint 4.2/4.2.1 stop well short of trick resolution). It becomes a REAL risk the moment any FUTURE sprint trusts `cardLog` as authoritative history for scoring, replay, or dispute resolution.
  - **`cardLog` is marked client-authoritative, MVP-only** as of this sprint. It is suitable for a soft-launch, honor-system, played-among-friends experience — the same tier `MigrationPlan.md` already assigns to `transactions/{txId}` and `leaderboards` stat writes — and explicitly **not** suitable for ranked or competitive play without further work.
  - **Two concrete, NOT-implemented-this-sprint directions for closing this gap**, recorded for whichever future sprint takes it on: (a) a Cloud Function that owns all `cardLog` writes server-side, re-deriving the append itself rather than trusting a client-submitted array (requires Blaze — explicitly out of Spark's reach); or (b) restructuring card plays as a `matches/{matchId}/plays/{autoId}` SUBCOLLECTION instead of an array field, with `allow create: if <the same ownership/shape checks>` and `allow update, delete: if false` — Firestore's own native "a created document can never be updated or deleted" guarantee would achieve TRUE prefix immutability for free, with no CEL list-comparison trick needed at all, and remains fully Spark-compatible. Recommended as the more promising of the two, but a genuine redesign — not attempted in this hotfix, per its own explicit "do not redesign" scope.

## Where rules-only enforcement is a real guarantee vs. a soft approximation

Stated plainly, because pretending otherwise would undercut the whole design:

- **Strong (rules alone are sufficient):** ownership checks (`request.auth.uid == uid`), structural checks (does this field exist, is it the right type), simple time-window checks (`dailyRewards`'s once-per-day clamp), "is this uid actually seated in this room/match," and — new as of Sprint 3.8 — **seat ownership and version-increment correctness for bidding AND card writes** (`isValidBidSubmission()`/`isValidCardSubmission()`): these are exact, structural facts (does `seats[seat] == request.auth.uid`, is `version` exactly `+1`), not approximations, so rules alone are fully sufficient for them, permanently, not just until Blaze.
- **Soft (rules approximate, real enforcement needs Cloud Functions later):**
  - **`matches/{matchId}` turn-order/legality.** Rules *can* check "the field you're writing corresponds to the seat whose turn it currently is" (Sprint 3.8 does exactly this for WHO may submit a bid), but cannot re-run the full `TableEngine`/`BiddingEngine` legality logic (follow-suit rules, Forbidden-13, Sa'ayda escalation math, or — directly relevant to this sprint — whether a submitted bid VALUE is itself a legal bid under the auction rules) — a sufficiently determined client could still submit a structurally-valid but game-illegal bid value (e.g. a nonsensical trick count), since `submitBid()`/`isValidBidSubmission()` deliberately treat `bid` as an opaque payload (see `docs/architecture/MatchSynchronization.md`'s Sprint 3.8 section) — validating that remains `bidding-engine.js`'s job, untouched this sprint, and is not yet wired to this write path at all. Acceptable for a soft launch among friends; **explicitly called out in `MigrationPlan.md` as a Ranked-Match blocker.** Card VALUE legality is different (see "Card write authority" above) — Sprint 4.2.1 DID close that gap, but did so CLIENT-side (`submitCard()`'s own pre-write `canPlayCard()` check), not via `firestore.rules`, since expressing follow-suit logic in CEL would mean duplicating `table-engine.js`'s own rules — exactly what this whole project forbids.
  - **`matches/{matchId}.cardLog` prefix integrity (Sprint 4.2.1's Task 4 finding).** `isValidCardSubmission()` cannot prove earlier log entries are unchanged or unreordered — see "Card write authority" above for the full, demonstrated account. This is a DIFFERENT kind of soft spot than the others in this list: it's not "rules approximate a formula," it's "rules cannot express this check at all with CEL's current primitives" — closing it needs either a Cloud Function or restructuring `cardLog` as an append-only subcollection (both documented above, neither built this sprint).
  - **`leaderboards`/`players` stat writes.** The "clamp the delta to the match's actual stakes" rule is only as good as the rules author's ability to encode the scoring formula in rules syntax — realistically an approximation (e.g. "RP change this write can't exceed X") rather than an exact re-derivation of `ScoringEngine`'s output.
  - **`transactions/{txId}`.** This is the honest weak point: a client that can create its own transaction record can, in principle, fabricate one claiming a purchase happened. For soft-currency-only transactions the blast radius is small (a few fake in-game coins). **This collection should be the first thing migrated to Cloud-Functions-only writes**, ahead of everything else in this table, the moment Blaze is available — see `MigrationPlan.md`.

## What does NOT need Cloud Functions, ever

Worth stating explicitly so "postpone until Blaze" doesn't get over-applied: ownership-scoped reads/writes (the bulk of this table), the `dailyRewards` time-clamp, room seat/ready management, and friend request accept/decline are all fully and permanently satisfiable by rules alone — these are not "temporary until Blaze," they're the correct permanent design.
