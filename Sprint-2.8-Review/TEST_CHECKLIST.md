# Test Checklist — Sprint 2.8: Player Profile Integration

| # | Test | Result | Evidence |
|---|---|---|---|
| 1 | Logged (full-account) user: Lobby fetches and displays the real profile | **PASS** (via stub) / **NOT PERFORMED** (live) | Real browser test: seeded `GameState` with a distinct local name/rank/coins/gems, injected a `PlayerService` stub resolving a *different* known profile, confirmed the rendered DOM shows the stub's values, not the seeded ones — proves the actual shipped fetch-and-reapply code works. Not yet re-verified against the live Firestore project: `firestore.rules` from Sprint 2.6 is still not deployed, so a real fetch today returns permission-denied regardless of this sprint's code correctness. |
| 2 | Guest user: same behavior as a full account | **PASS** | Same test pattern, run with `accountType: "guest"` and no email — the integration code doesn't branch on account type at all, so this is the same code path as test 1; confirmed via a real browser test with a guest-shaped seed and a profile-resolving-`null` stub (see test 3). |
| 3 | Missing profile (`PlayerService.getPlayerProfile` resolves `null`): safe defaults shown, no crash | **PASS** | Real browser test: stub resolves `null`; confirmed the DOM keeps the locally-seeded values exactly as they were, no console error, no exception. |
| 4 | Offline/unavailable Firestore: safe defaults shown, Lobby continues loading normally | **PASS** | Two independent real tests: (a) stub rejects with a simulated error — confirmed caught cleanly, local values preserved, no crash; (b) the actual unmodified shipped `lobby/index.html` + real `firebase-init.js`/`player-service.js`, run in this sandbox where the Firebase CDN is genuinely unreachable (the same limitation established in Sprint 2.6) — confirmed the real code's own error path fires (`"Firestore is not initialized"`), caught, local values preserved, Lobby fully rendered and interactive. |
| 5 | Lobby still loads (structurally, visually) in every scenario above | **PASS** | Confirmed via screenshot in all four scenarios above — topbar, masthead, mode cards, Season/Missions panel all render identically regardless of which profile-fetch outcome occurred. |
| 6 | Display Name, Rank, Coins, Gems are bound to the real profile when available | **PASS** | Covered by test 1's assertion (stub values appeared verbatim in the DOM for all four fields). |
| 7 | RP and Level are bound | **NOT PERFORMED** | No existing UI element for either field exists anywhere in Lobby's current markup — binding them would require adding new elements, which conflicts directly with "do not redesign Lobby, do not move components." Flagged honestly in `PlayerProfileIntegration.md` rather than fabricating markup or silently omitting the requirement. |
| 8 | Lobby never calls Firestore or Auth directly | **PASS** | Repo-wide search of `design-ui/lobby/index.html` for `firebase.firestore(`, `window.Db`, `.collection(`, `firebase.auth(`, `window.Auth` — zero matches outside the `<script src>` URLs themselves. Lobby also never loads `firebase-auth-compat.js` at all, so there is no way for it to access Auth even by accident. |
| 9 | `PlayerService` was not modified | **PASS** | `git diff` — `design-ui/player-service.js` does not appear in this sprint's change set. |
| 10 | Login, GameState, GameSession, and all gameplay engine files are untouched | **PASS** | `git diff` — `design-ui/login/index.html`, `login/game-state.js`, `lobby/game-state.js`, and every file under `design-ui/engine/` do not appear in this sprint's change set. |
| 11 | No new Firestore collections, rules changes, or Blaze-only features were introduced | **PASS** | Confirmed by reading the full diff — the only Firestore-related code added is a single `getPlayerProfile()` call, already covered by Sprint 2.6's existing `players/{uid}` scope; nothing else touches Firestore, and no rules file was changed. |

## What this means practically

Test 1's `NOT PERFORMED` (live) qualifier and test 7's gap are the only open items, and neither blocks review: test 1 is waiting on the same pending manual rules-deploy step from Sprint 2.6 (not new to this sprint), and test 7 is an honest scope conflict between the stated objective and the "do not redesign" constraint, not an oversight — resolving it is a UI decision for the next design pass, not a code fix.
